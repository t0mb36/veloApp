# Development Dockerfile for hot reloading with Vite
FROM node:20-alpine AS development

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Expose vite dev server port
EXPOSE 3000

# Start vite dev server
CMD ["npm", "run", "dev"]
